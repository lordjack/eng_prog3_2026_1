<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /APIModelo/public/
    
    # Se não for um arquivo ou diretório, redireciona para index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
